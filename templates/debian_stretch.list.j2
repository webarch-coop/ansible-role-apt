# {{ ansible_managed }}

# Debian {{ apt_distro }} repos
deb https://deb.debian.org/debian {{ apt_distro }} {% for apt_comp in apt_debian_components %}{{ apt_comp }}{% if not loop.last %} {% endif %}{% endfor %}

deb https://deb.debian.org/debian {{ apt_distro }}-updates {% for apt_comp in apt_debian_components %}{{ apt_comp }}{% if not loop.last %} {% endif %}{% endfor %}

deb https://deb.debian.org/debian-security {{ apt_distro }}/updates {% for apt_comp in apt_debian_components %}{{ apt_comp }}{% if not loop.last %} {% endif %}{% endfor %}

# vim: ft=debsources
